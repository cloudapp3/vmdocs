---
title: vmflow tui
description: 针对一个运行中的守护进程启动 vmflow 终端仪表盘。
---

# vmflow tui

```bash
vmflow tui [-token TOKEN]
```

别名：`vmflow t`。

启动连接本机守护进程的终端仪表盘。视图与操作参见 [TUI 仪表盘](./tui-guide)。

## 示例

```bash
vmflow tui
VMFLOW_CONTROL_TOKEN=change-me vmflow tui
```
